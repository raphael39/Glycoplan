class RecipeDetails {
    constructor(id, title, color, ingredients, steps, persons) {
      this.id = id;
      this.title = title;
      this.color = color;
      this.ingredients = ingredients;
      this.steps = steps;
      this.persons = persons;
    }
  }
  
  export default RecipeDetails;